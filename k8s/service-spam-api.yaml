apiVersion: v1
kind: Service
metadata:
  name: spam-api
spec:
  type: NodePort
  selector:
    app: spam-api      # matches the pod template labels
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
